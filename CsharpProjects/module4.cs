/*string[] fraudulentOrderIDs = new string[3];  ### this how i declarte array of dting with 3 elements, 0,1,2
*/

string[] names = { "jihad", "lewii", "Brahim" };
foreach (string name in names)
{
    Console.WriteLine(name);
}
